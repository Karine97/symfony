<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Delete</button>
</form>

<!--_delete_form.html.twig : ce fichier contient le formulaire pour supprimer un utilisateur. Il est inclus dans le fichier show.html.twig. -->